# Legacy Archive: runbook.md

## Archive reason

The old aggregate `docs/runbook.md` mixed current Reliability Demo API response steps with legacy AI Moving Assistant / Moving Prep operations.

It included old references such as:

```text
- AI Moving Assistant API
- POST /api/moving-assistant
- old API safety validation for the moving-assistant endpoint
```

The current active runbook source is:

```text
docs/runbooks/reliability-demo-api.md
```

## Current treatment

```text
Historical only.
Do not use this archive file as the active incident response runbook.
```
